# Flutter environment configuration
# Copy to .env before running

# Base URL of the Go API server
API_BASE_URL=http://localhost:8080

# API key (copy from the server's first-run output)
API_KEY=your-api-key-here
